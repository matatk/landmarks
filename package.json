{
  "name": "landmarks",
  "version": "2.0.4",
  "private": true,
  "scripts": {
    "prebuild": "eslint .",
    "build": "node scripts/build.js all",
    "clean": "rimraf build/firefox && rimraf build/chrome",
    "predeepclean": "npm run clean",
    "deepclean": "rimraf build/png-cache/*"
  },
  "devDependencies": {
    "chalk": "~1.1",
    "deepmerge": "~1.3",
    "eslint": "~3.11",
    "fs-extra": "~1.0",
    "rimraf": "~2.5",
    "svg2png": "~4.1"
  }
}
